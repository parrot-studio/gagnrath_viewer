<div class="btn-toolbar">
  <div class="btn-group">
    <%= link_to '6weeks', root_path, :class => "btn btn-info" %>
  </div>
  <div class="btn-group">
    <a href="#" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">Rulers <b class="caret"></b></a>
    <ul class="dropdown-menu">
      <% @gvdates.each do |d| %>
        <li><%= link_to divided_date(d), result_path(d) %></li>
      <% end %>
    </ul>
  </div>
  <div class="btn-group">
    <a href="#" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">Caller <b class="caret"></b></a>
    <ul class="dropdown-menu">
      <% @gvdates.each do |d| %>
        <li><%= link_to divided_date(d), ranker_path(d) %></li>
      <% end %>
    </ul>
  </div>
  <div class="btn-group">
    <%= link_to 'About', about_path, :class => 'btn' %>
  </div>
</div>
